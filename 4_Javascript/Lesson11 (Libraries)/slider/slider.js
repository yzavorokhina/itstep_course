$(document).ready(function(){
    let options = {
        margin: 10,
        loop: true,
        // autoplay: true,
        // autoWidth: true,
    }
    $(".owl-carousel").owlCarousel(options);
  });